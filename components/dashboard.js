import React from 'react';
import component from 'omniscient';

var Component = component('Dashboard', (props) => {

  return (
    <div>
      <h2>Dashboard</h2>
    </div>
  );

}).jsx;

export default Component;
