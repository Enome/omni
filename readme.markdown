# Omniscient + Immustruct + React Router

Application state keeps track of:

- Routes (two way)
- Mouse position (one way)
- Scroll position (two way)


## Installation

```sh
npm i
webpack --devtool source-map
```

Run a web server.
